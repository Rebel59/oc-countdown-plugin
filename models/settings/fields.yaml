# ===================================
#  Form Field Definitions
# ===================================

fields:
    date:
        label: rebel59.countdown::lang.settings.date
        type: datepicker
        mode: datetime
    comment:
        label: rebel59.countdown::lang.settings.comment
        type: richeditor
        size: huge